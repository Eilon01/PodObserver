apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-observer.fullname" . }}
  labels:
    {{- include "pod-observer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pod-observer.selectorLabels" . | nindent 4 }}
